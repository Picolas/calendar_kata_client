import { IsMultipleDayPipe } from './is-multiple-day.pipe';

describe('IsMultipleDayPipe', () => {
  it('create an instance', () => {
    const pipe = new IsMultipleDayPipe();
    expect(pipe).toBeTruthy();
  });
});
